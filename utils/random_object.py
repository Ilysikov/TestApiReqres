from utils import hobbies_, picture_, first_name, las_name, email_, gender_, mobile_, date_, subject_, states_, \
    address_

# class UserData:
#
#     def __init__(self):
#         super().__init__()
#
#         self.random_user_data= {'id': self.input_int(20,80), 'email': self.valid_email(), 'first_name': self.,
#                  'last_name': 'Morris',
#                  'avatar': 'https://reqres.in/img/faces/5-image.jpg'}
#             return {"average": str(random_string()), "reviews": random_string()}